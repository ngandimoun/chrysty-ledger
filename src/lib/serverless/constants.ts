export const SERVERLESS_TIMEOUT_MESSAGE =
  "This request took too long and was stopped. Try a shorter message, fewer files, or retry.";

export const SERVERLESS_TIMEOUT_CODE = "SERVERLESS_TIMEOUT";

export const TOOL_LOOP_MIN_BUDGET_MS = 15_000;
